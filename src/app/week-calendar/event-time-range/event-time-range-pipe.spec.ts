import { EventTimeRangePipe } from './event-time-range-pipe';

describe('EventTimeRangePipe', () => {
  it('create an instance', () => {
    const pipe = new EventTimeRangePipe();
    expect(pipe).toBeTruthy();
  });
});
